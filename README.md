### Basic Toggle Switch and Timer React Hooks

Basic React App that has a toggle and a timer built using Hooks.

To run the App, type **npm start** in the terminal that will open a tab to http://localhost:3000
